# Force LF for all text files so Windows checkouts (core.autocrlf=true,
# e.g. GitHub Actions windows runners) don't break gofmt, which expects LF.
* text=auto eol=lf

# Windows scripts genuinely need CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
